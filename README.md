# Polylog

Public static dashboard export for the Polymarket BTC paper-trading runner.

- `index.html` is the static dashboard.
- `summary.json` is the sanitized public data snapshot.
- Local runner logs, PID files, and raw API error messages are not published.
